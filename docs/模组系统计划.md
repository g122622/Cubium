# 模组系统计划

## 概述

实现两套模组系统

1. 第一套：完整兼容基岩版addon
2. 第二套：基于C++的模组系统，提供更强大的功能和性能(兼容LeviLamina)（先不做，本次任务goal只做第一套）

## 基岩版addon兼容模组系统

必须完整参考并精准复刻基岩版文档：`E:\dev\MC\Mods\minecraft-creator`中的api和行为

为了随时切换js引擎，需要将js引擎抽象成接口（目前先暂时只使用quickjs，未来可能切换到v8以做性能对比或优化）

Editor先不做。

## 参考资料

参考资料1：必须阅读 E:\dev\MC\LeviLamina\mc-addon-script-architecture.md

参考资料2：mc逆向源码（仅头文件），读完参考资料1后，必须派出至少两个子代理探索：`E:\dev\MC\LeviLamina\src\mc` `E:\dev\MC\LeviLamina\src-client\mc` `E:\dev\MC\LeviLamina\src-server\mc`，看看参考资料1是否有错误和遗漏

---

## 实现进度

### 阶段1：基础设施 ✅ 已完成

#### 1.1 QuickJS引擎集成 ✅
- `core/` — IScriptRuntime/Engine/Context抽象接口
- `engine/quickjs/` — QuickJSRuntime/Engine/Context实现、QuickJSModuleLoader、QuickJSValueConvert

#### 1.2 行为包系统 ✅
- `pack/` — AddonManifest（manifest.json解析）、BehaviorPack、BehaviorPackList、PackDependencyResolver、PackVersion、AddonModule、AddonDependency

#### 1.3 事件系统扩展 ✅
- `event/` — ScriptEventBus、BeforeEventSignal、AfterEventSignal
- ServerEventBus cancellation支持

### 阶段2：模块绑定框架 + @minecraft/server核心绑定 ✅ 已完成

#### 2.1 模块绑定框架 ✅
- `binding/` — IModuleBindingFactory、ModuleBinding、ClassBinding、ScriptClassBinding、TypeConverter、ScriptObjectHandle

#### 2.2 @minecraft/server核心绑定 ✅
- `modules/` — MinecraftModuleFactory及World/System/Dimension/Entity/Player/Block/ItemStack等绑定
- 事件绑定（BeforeEvents/AfterEvents）
- 共享类型（Vec3/Vec2/Color/Direction/GameMode）

### 阶段3：插件生命周期 + 服务端集成 ✅ 已完成

#### 3.1 插件管理 ✅
- `plugin/` — ScriptPlugin（状态机）、ScriptPluginManager、ScriptPluginSource、ScriptPackPermissions、ScriptPackConfiguration、PluginExecutionGroup

#### 3.2 服务端集成 ✅
- `lifecycle/` — ScriptManager、ScriptTickListener、ScriptWatchdog、ScriptLogger
- `diagnostics/` — ScriptDiagnostics、ScriptSentryLogger
- `src/server/mod/bedrock/addon/` — ServerScriptManager
- MinecraftServer集成（初始化、tick驱动、关闭）

### 阶段4：测试 + 文档 + 清理 ✅ 已完成

- 单元测试：test_lifecycle.cpp（31个测试用例）
- README文档：各子目录README.md
- 构建验证：所有目标编译通过

### 阶段5：自定义组件系统 ✅ 已完成

- `component/` — BlockComponentRegistry、ItemComponentRegistry、事件数据结构、CustomComponentParameters
- 方块事件：StepOn、StepOff、Place、Break、PlayerBreak、PlayerInteract、PlayerPlaceBefore（可取消）、EntityFallOn、RandomTick、Tick、Entity、RedstoneUpdate、BlockStateChange
- 物品事件：Use、UseOn、HitEntity、BeforeDurabilityDamage（可修改damage）、MineBlock、CompleteUse、Consume
- JS绑定：ScriptCustomComponentBinding（blockComponentRegistry/itemComponentRegistry全局对象）
- 性能优化：位掩码CallbackFlags快速判断回调存在性
- 单元测试：test_block_component_registry.cpp（22个用例）+ test_item_component_registry.cpp（21个用例）

### 阶段6：核心API绑定实现（当前阶段）

**目标**：将阶段2中的stub实现替换为连接真实游戏对象的完整实现。

#### 6.1 System调度器 ✅
- `system.run(callback)` — 延迟到下一tick执行回调
- `system.runInterval(callback, tickInterval)` — 每隔N个tick执行回调
- `system.runTimeout(callback, tickDelay)` — 延迟N个tick后执行一次
- `system.clearRun(runId)` — 取消已注册的调度
- `system.currentTick` — 返回当前服务器tick（待集成ScriptWorld）
- 实现：`ScriptScheduler`类（lifecycle/ScriptScheduler.hpp/.cpp）
- 通过`ModuleContextData`注入调度器指针到JS上下文
- ServerScriptManager.tick(currentTick)驱动调度器

#### 6.2 World API绑定 🔄
- `world.afterEvents` / `world.beforeEvents` — 事件订阅代理对象
- `world.getDimension(dimensionId)` — 返回ScriptDimension
- `world.getAllPlayers()` — 返回Player数组
- `world.sendMessage(message)` — 广播聊天消息
- `world.scoreboard` — 记分板访问
- `world.gameRules` — 游戏规则读写
- `world.getTimeOfDay()` / `world.setTimeOfDay()`
- `world.getDynamicProperty()` / `world.setDynamicProperty()`
- `world.structureManager` — 结构模板管理

#### 6.3 Dimension绑定
- ScriptDimension包装类，持有DimensionId/Dimension引用
- `dimension.id` — 维度标识符
- `dimension.getBlock(location)` — 获取方块
- `dimension.getEntities(options)` — 实体查询
- `dimension.getPlayers(options)` — 玩家查询
- `dimension.spawnEntity(identifier, location)` — 生成实体
- `dimension.runCommand(commandString)` — 执行命令

#### 6.4 Entity/Player绑定
- ScriptEntity包装类，持有Entity*引用
- 属性：id, typeId, location, velocity, rotation, dimension
- 方法：teleport(), kill(), remove(), runCommand(), getComponent(), addEffect(), removeEffect()
- ScriptPlayer继承ScriptEntity
- 额外属性：name, gameMode, isSneaking, isJumping, selectedSlot
- 额外方法：sendMessage(), getInventory()

#### 6.5 Block绑定
- ScriptBlock包装类，持有BlockPos + DimensionId
- 属性：location, permutation, type, isWaterlogged, dimension
- 方法：getComponent(), setPermutation(), getBlockState()

#### 6.6 ItemStack绑定
- ScriptItemStack包装类
- 属性：typeId, amount, nameTag, lore, durability, maxDurability
- 方法：getComponent(), clone(), isStackable()

#### 6.7 事件桥接（ServerEventBus → ScriptEventBus）
- 创建EventBridge系统，将ServerEventBus事件转换为脚本事件
- BeforeEvents：entityHurt, playerBreakBlock, playerPlaceBlock, playerInteractWithBlock, chatSend, explosion, weatherChange, itemUse
- AfterEvents：entityDie, entitySpawn, playerJoin, playerLeave, blockExplode, effectAdd
- 每个事件需要：数据转换（C++事件结构 → JS事件对象）、订阅管理、生命周期绑定

#### 6.8 共享类型实现
- ScriptVec3 — 三维向量
- ScriptVec2 — 二维向量
- ScriptColor — RGBA颜色
- ScriptDirection — 方向枚举
- ScriptGameMode — 游戏模式枚举
- ScriptEntityQueryOptions — 实体查询选项

### 阶段7：@minecraft/server-ui模块

- `modules/ui/` — 新模块目录
- ScriptMinecraftServerUIModuleFactory — 模块工厂
- ActionFormData — 按钮选择表单
- ModalFormData — 复杂输入表单（dropdown, slider, textField, toggle）
- MessageFormData — 双按钮确认对话框
- FormPromiseTracker — 追踪表单Promise（玩家→服务器响应）
- 表单响应类型：ActionFormResponse, ModalFormResponse, MessageFormResponse
- 依赖：@minecraft/server

### 阶段8：Dynamic Properties持久化

- `modules/persistence/` — 持久化模块
- ScriptDynamicPropertiesDefinition — 属性定义（defineBoolean/Number/String/Vector3）
- ScriptPropertyRegistry — 注册实体/世界属性
- World级：getDynamicProperty/setDynamicProperty/getDynamicPropertyIds
- Entity级：同上
- 跨世界持久化：存储到LevelSave
- 字节计数限制

### 阶段9：Structure Manager API

- `modules/structure/` — 结构模板模块
- ScriptStructureManager — createEmpty/createFromWorld/get/delete/place
- ScriptStructureTemplate — getBlockPermutation/setBlockPermutation/saveAs/saveToWorld
- StructurePlaceOptions — includeEntities, includeBlocks, rotation, mirror, animationMode
- 枚举：StructureRotation, StructureMirrorAxis, StructureAnimationMode, StructureSaveMode

### 阶段10：Spawn Rules + 高级特性

- CustomSpawnRules — 脚本自定义生成规则
- EntitySpawnCallback / ObstructionCallback
- FormPromiseTracker完整实现
- AsyncJobCoordinator — 异步任务协调
- /reload命令完整实现
- Watchdog完整实现（超时终止脚本上下文）

---

## 架构设计要点

### 事件桥接架构

```
ServerEventBus (C++游戏事件)
    │
    ├── BeforeEventBridge ──→ ScriptEventBus ──→ BeforeEventSignal ──→ JS回调(可取消)
    │                                                                    │
    │                                          HandlerResult::BypassListeners
    │                                          → CoordinatorResult::Cancel
    │
    └── AfterEventBridge ──→ ScriptEventBus.enqueueAfterEvent()
                                    │
                              Tick结束时flush ──→ AfterEventSignal ──→ JS回调(只读)
```

### 游戏对象包装架构

```
C++游戏对象 (Entity*, Block*, Dimension*, ...)
    │
    ├── ScriptObjectHandle ──→ JS对象生命周期管理
    │
    ├── ScriptEntity ──→ JS Entity对象
    │     └── 持有 EntityId/Entity* 引用
    │     └── 属性/方法通过TypeConverter桥接
    │
    ├── ScriptBlock ──→ JS Block对象
    │     └── 持有 BlockPos + DimensionId
    │
    └── ScriptItemStack ──→ JS ItemStack对象
          └── 持有 ItemStack数据副本（值语义）
```

### System调度器架构

```
ScriptTickListener
    │
    ├── m_pendingRuns ──→ std::vector<ScheduledCallback> (system.run)
    ├── m_intervalRuns ──→ std::unordered_map<RunId, IntervalCallback> (system.runInterval)
    ├── m_timeoutRuns ──→ std::unordered_map<RunId, TimeoutCallback> (system.runTimeout)
    │
    └── tick() 每tick执行:
          1. 处理m_pendingRuns
          2. 检查m_timeoutRuns到期项
          3. 检查m_intervalRuns到期项
          4. 递增m_currentTick
```

---

## 未实现功能（后续迭代）

- [ ] @minecraft/server-ui（阶段7）
- [ ] @minecraft/server-net
- [ ] @minecraft/server-gametest
- [ ] @minecraft/server-admin
- [ ] @minecraft/server-editor
- [x] 自定义方块组件（BlockCustomComponent + BlockComponentRegistry + JS绑定）
- [x] 自定义物品组件（ItemCustomComponent + ItemComponentRegistry + JS绑定）
- [ ] 自定义生成规则（CustomSpawnRules）（阶段10）
- [ ] 客户端脚本（ClientScriptManager）
- [ ] 调试器支持（ScriptDebugger）
- [ ] V8引擎后端
- [ ] 脚本热重载（/reload命令完整实现）
- [ ] Watchdog完整实现（超时终止脚本上下文）
- [ ] FormPromiseTracker（阶段7）
- [ ] AsyncJobCoordinator（阶段10）
- [ ] 结构模板操作（structureManager API）（阶段9）
- [ ] 动态属性持久化（跨世界保存）（阶段8）
