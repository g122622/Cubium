---
name: mc-new-feature
description: 参照mc源码，列出高质量计划，为项目实现新功能
---

## 任务介绍

你需要进入计划模式，为本项目补全用户指定内容。

具体而言，请你并行启动大量子代理进行探索（9个）（不要串行启动，很慢），然后阻塞等待所有子代理完成（禁止在后台运行），然后进入计划模式进行列出完备且详细的补全计划（但是不要启动plan agent，你必须亲自起草计划）！

【重要】不要启动plan agent，你必须亲自起草计划！
【重要】不要启动plan agent，你必须亲自起草计划！
【重要】不要启动plan agent，你必须亲自起草计划！

计划文件必须非常详细，包含尽可能多的信息，以便交接时顺利。

子代理的配置项必须如下：

`run_in_background`: false,
`model`: haiku,
`isolation`: 不要配置

需注意我提出的功能需求可能该项目已经部分实现，请务必充分探索避免重复实现。

这9个子代理中，需要有3-5个专门负责探索项目中已有的对应基建（只探索本次任务可能需要依赖的基建），以实现完美的、最高程度的复用和现有系统的集成。不要限制子代理的输出字数，让它们尽可能详细地输出它们的探索结果。

探索完毕之后，如果仍然有你不确定的细节、想了解的实现等，继续派出大量子代理进行探索，直到你完全理解为止！

注意本项目基建已经相当完善（代码量已经100w行），各种常数、常用工具函数、音频系统、粒子系统、资源包系统、命令系统、实体系统、物品系统、物理系统、碰撞、tick调度、存档等都已经有了相当完善的实现(另外world对象上面也挂了相当多的工具方法以便访问世界、操作世界等)，务必充分探索以实现复用，避免重复实现，或者以未实现为理由留下TODO。

撰写计划之前必须通过AskUserQuestion向用户确认需求细节，直到你完全理解用户的需求为止！如果你不确定用户的需求细节，请务必先向用户提问确认，而不是直接开始撰写计划！

## 善用README

几乎每个目录下都有一个简体中文编写的 README.md，它很重要，在你探索项目实现的时候，提前看看它，能节省不少多余的文件查找与查看，节省不少上下文开销。

【重要】为了节省上下文，请你尽量阅读各级目录的 README.md 来获取你需要的信息，而不是直接查看代码文件。

例如：当你修改想修改或查看`src\common\world\chunk\ChunkLoadTicketManager.cpp`，你必须提前依次查看目录链上的各级目录上的readme文件：

- 项目根目录的 README.md
- src\common\README.md
- src\common\world\README.md
- src\common\world\chunk\README.md

请你不仅自己要严格遵照上述要求，而且在给子代理的prompt中也要要求它们先查看相关目录链上的README.md来获取信息，而不是直接查看代码文件。

## 关于代码

【重要】必须参考下面mc源码！
windows环境源码位于 `D:\Minecraft\MC研究\Minecraft1.21.11源码`
macos环境源码位于 `~/MC_Dev/Minecraft1.21.11源码`

1. 必须优雅、考虑到未来拓展、结构清晰美观整洁、架构强大，必须参考mc源码（主要是为了学习mc真实游戏逻辑而非架构。mc的架构水平和代码质量并不高！），但不要照搬，必须根据cpp的特性和当前项目的架构进行合理的架构设计和调整。
2. 当前项目是一个复刻项目，目标是完全使用cpp尽可能一致地复刻java版mc的游戏体验，并在存档、数据包等层面上尽可能兼容和复用现有java版minecraft生态。务必要有清晰、优雅、能让人赏心悦目的目录结构，不要把很多文件全部堆在一个目录下，要划分好细分的子目录。
3. 你需要先阅读readme文件了解怎么构建项目
4. 需要断言+单测来保证代码质量；每个方法前都要附上doc注释说明方法的用法和注意事项（容易踩坑的地方）
5. 务必要有清晰、优雅、能让人赏心悦目的目录结构，不要把很多文件全部堆在一个目录下，要划分好细分的子目录！
6. 编译过程中遇到的warning你也要一并解决
7. 需要使用命名空间隔离各个子系统的标识符。下面是最佳实践：

```cpp
namespace mc {
namespace entity {
namespace attribute {

/**
 * @brief 属性修改器操作类型
 *
 * 定义属性修改器如何影响基础值
 */
enum class Operation : u8 {
    // ...
}}}}
```

## 任务结束后

代码编写结束之后，请你进行严格的多轮自查，检查是否完整实现、是否留有TODO、是否未完整集成到现有游戏流程中导致形成“孤岛代码”、测试用例是否完备且合理且足够复杂以便暴露问题、没用了的旧代码是否彻底清除、相关文档（readme）是否更新等。

最后，生成简体中文提交信息并提交代码。

提交代码之前，必须使用clang-format对你修改的文件进行格式化：

```
clang-format -i src\common\xxx\Foo.cpp
clang-format -i src\common\xxx\Foo.hpp
```
