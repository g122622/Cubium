# 测试素材（真实存档、二进制样本）：禁止换行转换，并禁用文本差异。
#
# 为什么必须显式声明：Windows 上 core.autocrlf=true 时，若某个素材被判定为文本，
# 检出阶段会改写其中的换行字节。.mca / .dat 内部多为压缩后的二进制，通常能被 git
# 自动识别为二进制；但含大量 ASCII 片段的 .dat（NBT 的字符串与键名）以及素材附带的
# .json 存在被误判为文本的余地。一旦被改写，读取测试会以"存档损坏"的形式失败，
# 而根因在检出配置上，排查成本极高。此处显式声明为二进制，一并省去 .mca 参与 diff。
tests/unit/testdata/** -text -diff
