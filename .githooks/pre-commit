#!/usr/bin/env bash

# set -euo pipefail

# # 提交前统一补齐许可证头，并将脚本改动纳入本次提交。
# node scripts/addLicense.mjs

# echo "pre-commit: 已补齐许可证头并暂存相关改动"

# # 仅处理已暂存的 C++ 源文件，避免在提交时无关改动被带入。
# mapfile -t files < <(git diff --cached --name-only --diff-filter=ACMR | grep -E '\.(cpp|hpp)$' || true)

# echo "pre-commit: 发现 ${#files[@]} 个已暂存的 C++ 文件需要格式化"

# if [ "${#files[@]}" -eq 0 ]; then
#     exit 0
# fi

# if ! command -v clang-format >/dev/null 2>&1; then
#     echo "pre-commit: clang-format 未安装或不在 PATH 中" >&2
#     exit 1
# fi

# for file in "${files[@]}"; do
#     if [ ! -f "$file" ]; then
#         continue
#     fi

#     clang-format -i -- "$file"
#     git add -- "$file"
#     echo "pre-commit: 已格式化并暂存 $file"
# done

# echo "pre-commit: 已格式化 ${#files[@]} 个已暂存的 C++ 文件"
